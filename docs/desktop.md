# Oulala — Desktop Install Instructions

You are Claude Code, and you've been asked to install Oulala — a personal AI assistant.

## Step 1: Clone the repo

```bash
git clone https://github.com/papay0/oulala.git ~/.oulala
```

If `~/.oulala` already exists, pull the latest:

```bash
cd ~/.oulala && git pull
```

## Step 2: Tell the user they're ready

"Oulala is installed! Open the Claude Code app on your phone, connect to this machine, and open `~/.oulala`. Start talking — your AI is ready.

Edit `~/.oulala/SOUL.md` to customize its personality."
