{
  "env": {
    "CLAUDE_AUTOCOMPACT_PCT_OVERRIDE": "90"
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "~/.oulala/bin/sync.sh pull 2>/dev/null; true"
          },
          {
            "type": "command",
            "command": "echo \"[Routine bootstrap: Read brain/routines.json now. For EACH routine with enabled=true, call CronCreate with its cron expression and prompt so the session's scheduler has them. CronCreate jobs are session-only, so this MUST happen at every session start. Do it silently — no need to narrate unless something fails.]\""
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo \"[Time: $(date '+%A, %Y-%m-%d %I:%M %p %Z')]\""
          },
          {
            "type": "command",
            "command": "echo \"[Proactive check — DEFAULT YES: Did the user mention ANY future event, decision, activity, plan, feeling, or commitment? Walk, errand, decision in progress, doctor, social plan, project, even small mentions like 'I'll call mom later' — all of it triggers a follow-up. Silently schedule a one-shot CronCreate with context + reply_to message_id. Bar is LOW. Over-proactive > under-proactive. Don't announce it, just set it like a friend who remembers.]\""
          },
          {
            "type": "command",
            "command": "echo \"[Channel rule: If from Telegram/Discord, text output is INVISIBLE — send ALL responses (ack, status updates, final result) via the channel plugin. Never respond with just text.]\""
          },
          {
            "type": "command",
            "command": "echo \"[Narration rule — CRITICAL: On channels (Telegram/Discord), every narration MUST be sent via the channel reply tool. Prose text you write above or between tool calls is INVISIBLE to the user — it never reaches their phone. A line like 'Transcribing...' typed as text is a black hole. To narrate, call the channel reply tool.\n\nWhen to narrate (each is a separate channel send, not prose):\n1. Voice message arrives → first action is a channel reply 'Transcribing 🎙️' BEFORE whisper\n2. Research/search → channel reply 'Looking up X...' BEFORE the tool call\n3. Multi-step work → channel reply announcing the plan BEFORE starting\n4. Between major phases → channel reply 'Found X, now doing Y' BEFORE the next phase\n5. On error → channel reply with what failed, don't fail silently\n6. Before the final answer → the final answer IS the closing channel reply\n\nBatching: for one logical step requiring many small tool calls, ONE announcement up front is enough — don't send a message per tool. Heuristic: channel never silent for >30s of work. Warm and brief, not formal. Error on the side of too many updates.]\""
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash|WebFetch|WebSearch|Task",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"[Channel reminder: YOU MUST send a channel status update before your next action unless: (a) you JUST sent a channel message less than ~30 seconds ago, OR (b) the very next action will be the final channel send with the answer. This is a hard rule, not a suggestion. Writing prose as text output is invisible to channel users.]\""
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "~/.oulala/bin/sync.sh push 2>/dev/null; true"
          }
        ]
      }
    ]
  }
}
